<%= form_for(tipo_cliente) do |f| %>
  <% if tipo_cliente.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(tipo_cliente.errors.count, "error") %> prohibited this tipo_cliente from being saved:</h2>

      <ul>
      <% tipo_cliente.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="box-body">
    <div class="col-xs-12">
      <%= f.label :nombre %>
      <%= f.text_field :nombre, class: "form-control" %>
    </div>

    <div class="col-xs-12 modal-footer" >
      <%= link_to "Cancelar", tipo_procesos_path, class: "btn btn-default", style:"color:black" %>
      <%= f.submit "Guardar", class: "btn btn-primary", id: "guardar" %>
    </div> 
  </div>
  
<% end %>
