<%= form_for(departamento, validate: true) do |f| %>
  <% if departamento.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(departamento.errors.count, "error") %> prohibited this departamento from being saved:</h2>

      <ul>
      <% departamento.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="box-body">
    <div class="field">
      <%= f.label :nombre %>
      <%= f.text_field :nombre, class: "form-control" %>
    </div>

    <div class="col-xs-12 modal-footer" >
      <%= link_to "Cancelar", departamentos_path, class: "btn btn-default", style:"color:black" %>
      <%= f.submit "Guardar", class: "btn btn-primary", id: "guardar" %>
    </div> 
  </div>
<% end %>
